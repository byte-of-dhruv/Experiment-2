# Experiment-2

Aim - Study of List in Python

Theory - 
List in Python is used to store multiple data in one variable.
append() command is used in pyhon to add an element to a list.
insert() command is used in python to add an element to a list on a specific address.
max() command for largest element in a list.
extend() for combining two or more lists.
min() command for miniest element in a list.
len() command for no. of elements in a list.
sum() command for adding all the integers in a list.
sorted() for arranging elements in list in ascending. only used inside print function.
sort() for arranging elements in list in ascending. Used outside print function.
remove() for removing an element in a list. 


Algorithm -
1. list_1 = []

2. Printing specific elements
   list_1 = []
   print(list_1[])

3. For adding elements
   list_1.append()

4. For inserting
   list1.insert()

5. For combining lists
   list_1.extend(list_2)

6. 

